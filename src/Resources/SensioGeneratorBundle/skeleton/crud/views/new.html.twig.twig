{% block extends %}
{{ "{% extends '" ~ bundle ~ ":" ~ layout_subfolder ~ ":" ~ layout_filename ~ "' %}" }}
{% endblock extends %}

{% block body %}
{{ "{% block body %}" }}
{{ '{{ form_start(form) }}' }}
    <div class="head-content">
{% set hide_edit, hide_delete = false, true %}
{% include 'crud/views/others/record_actions.html.twig.twig' %}
        <div class="page-header">
            <h3>{{ "{{ '"~ real_entity ~" creation'|trans }}" }}</h3>
        </div>
    </div>

{{ '{{ form_widget(form) }}' }}
{{ '{{ form_end(form) }}' }}
{{ "{% endblock %}" }}
{% endblock body %}
